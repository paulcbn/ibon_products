import { makeStyles } from '@material-ui/core';


export const useHeaderStyle = makeStyles(theme => {

});

export const useHeaderCellStyle = makeStyles(theme => {

});
export const useTableDataStyle = makeStyles(theme => {

});
export const useTableWidgetStyles = makeStyles(theme => ({
  tableContainer: {
    position: 'relative',
  },
}));

export const useToolbarStyle = makeStyles(theme => ({
  title: {
    flex: '1 1 100%',
  },
}));